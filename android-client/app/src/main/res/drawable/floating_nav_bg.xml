<?xml version="1.0" encoding="utf-8"?>
<!-- Floating nav bar — frosted glass, slightly lighter than background -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="28dp" />
    <!-- Slightly lighter than #141218 background to show as frosted layer -->
    <solid android:color="#E8252230" />
    <stroke
        android:width="1dp"
        android:color="#18D0BCFF" />
</shape>
