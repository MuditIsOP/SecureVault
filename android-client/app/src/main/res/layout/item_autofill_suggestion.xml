<?xml version="1.0" encoding="utf-8"?>
<!--
  Autofill suggestion dropdown item — PRD F-AUTO-01 AC#2
  Used by RemoteViews in VaultAutofillService for credential suggestions.
  Shows credential name and username in the system autofill dropdown.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:padding="12dp"
    android:background="?android:attr/selectableItemBackground">

    <!-- SecureVault icon placeholder -->
    <ImageView
        android:id="@+id/iv_autofill_icon"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:src="@drawable/ic_lock"
        android:contentDescription="SecureVault" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginStart="12dp">

        <!-- Credential name -->
        <TextView
            android:id="@+id/tv_autofill_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="15sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end" />

        <!-- Username/Email -->
        <TextView
            android:id="@+id/tv_autofill_username"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:maxLines="1"
            android:ellipsize="end" />

    </LinearLayout>

</LinearLayout>
