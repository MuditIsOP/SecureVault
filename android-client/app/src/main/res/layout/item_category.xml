<?xml version="1.0" encoding="utf-8"?>
<!--
  Category list item layout — SCR-VLT-04 content inventory
  Design.md §2.1: surface #1C1B1F, on-surface #E6E1E5, secondary #CCC2DC
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    app:cardBackgroundColor="@color/color_surface"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="16dp">

        <!-- Category name — Body Large 16sp -->
        <TextView
            android:id="@+id/tv_category_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@color/color_on_surface"
            android:textSize="16sp"
            android:textStyle="bold" />

        <!-- Password count — Label Large 14sp, secondary color -->
        <TextView
            android:id="@+id/tv_password_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/color_secondary"
            android:textSize="14sp"
            android:paddingEnd="16dp" />

        <!-- Default badge — visible for predefined categories -->
        <TextView
            android:id="@+id/tv_default_badge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Default"
            android:textColor="@color/color_primary"
            android:textSize="10sp"
            android:paddingEnd="8dp"
            android:visibility="gone" />

        <!-- Delete button — hidden for default categories (edge case) -->
        <ImageButton
            android:id="@+id/btn_delete_category"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@android:drawable/ic_menu_delete"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Delete category"
            android:visibility="visible" />

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
